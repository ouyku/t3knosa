# decisions

# image search API

- serpapi —  100 free requests/month

**alternatives considered:**
- google lens — no public API
- google reverse image search — no public API
- cloud vision API — analyzes images, does not find them

**tried**
- google custom search api - did not work